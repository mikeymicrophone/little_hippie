Some colors are very similar and in some cases identical.  Different brands have different names.  Choose color according to color listed on PO, not by appearance.
<%= form_tag choose_product_colors_path do %>
  <%= hidden_field_tag :product_id, @product.id %>
  <% Color.without_product(@product).alphabetical.in_groups_of(4, false).each do |color_group| %>
    <div class="color_group">
      <% color_group.each do |color| %>
        <div class="color_grid">
          <%= check_box_tag "product_colors[#{color.id}]" %>
          <%= color.name %>
          <div style="width:100px;height:15px;background-color:#<%= color.css_hex_code %>;" class="swatch"></div>
        </div>
      <% end %>
    </div>
  <% end %><br>
  <%= submit_tag 'Add these colors' %>
<% end %>