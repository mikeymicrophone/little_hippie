$('.customer_shipping_address').append('<%= escape_javascript @shipping_addresses.map { |sa| div_for(sa) {sa.display + link_to(image_tag(asset_path('X_button.gif')), shipping_address_path(sa), :method => :delete, :remote => true)} }.join.html_safe %>');