<%= form_for(@mailing_list_registration) do |f| %>
  <% if @mailing_list_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailing_list_registration.errors.count, "error") %> prohibited this mailing_list_registration from being saved:</h2>

      <ul>
      <% @mailing_list_registration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
