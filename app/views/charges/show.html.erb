<p id="notice"><%= notice %></p>

<p>
  <b>Cart:</b>
  <%= link_to @charge.cart.id, @charge.cart %>
</p>

<p>
  <b>Amount:</b>
  <%= @charge.amount %>
</p>

<p>
  <b>Token:</b>
  <%= @charge.token %>
</p>


<%= link_to image_tag('edit_button.gif'), edit_charge_path(@charge) %> |
<%= link_to image_tag('back_button.gif'), charges_path %>
