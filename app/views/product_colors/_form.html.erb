<%= form_for(@product_color) do |f| %>
  <% if @product_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_color.errors.count, "error") %> prohibited this product_color from being saved:</h2>

      <ul>
      <% @product_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.select :product_id, options_from_collection_for_select(Product.all, :id, :name, @product_color.product_id) %>
  </div>
  <div class="field">
    <%= f.label :color_id %><br />
    <%= f.select :color_id, options_from_collection_for_select(Color.alphabetical, :id, :name, @product_color.color_id) %>
  </div>
  <div class="field">
    <%= f.label :og_code %><br>
    <%= f.text_field :og_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
