<%= form_for(@inventory_list) do |f| %>
  <% if @inventory_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_list.errors.count, "error") %> prohibited this inventory_list from being saved:</h2>

      <ul>
      <% @inventory_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :xml_file %><br />
    <%= f.file_field :xml_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
