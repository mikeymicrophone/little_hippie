<h1>Colors</h1>

<table>
  <tr>
    <th><%= link_to 'Name', :sort => 'name' %></th>
    <th><%= link_to 'Code', :sort => 'code' %></th>
    <th></th>
    <th></th>
    <th></th>
    <th><%= link_to 'New Color', new_color_path, :class => 'btn btn-success' %></th>
  </tr>
<br><br>Some colors are very similar and in some cases identical.<br>
Different brands have different names.<br>
Choose color according to color listed on PO, not by appearance.<br><br>
<% @colors.each do |color| %>
  <tr>
    <td><%= link_to_name color %></td>
    <td><%= color.code %></td>
    <td><div style="width:30px;height:30px;background-color:#<%= color.css_hex_code %>;" class="swatch"></div></td>
    <td><%= color.css_hex_code %></td>
    <td><%= link_to pluralize(color.product_colors.count, 'product color'), color_product_colors_path(color) %></td>
    <td><%= link_to pluralize(color.products.count, 'product'), color_products_path(color) %></td>
    <td><%= link_to pluralize(color.body_styles.count, 'body style'), color_body_styles_path(color) %></td>
    <td><%= link_to pluralize(color.designs.count, 'design'), color_designs_path(color) %></td>
    <td><%= color.product_colors.map { |product_color| link_to image_tag(product_color.product.primary_image(:email_receipt), :style => "background-color: ##{color.css_hex_code}; width: 40px;"), product_color }.join.html_safe %></td>
    <td>
      <%= link_to image_tag('up_button.gif'), move_up_color_path(color), :method => :put %>
      <%= link_to image_tag('down_button.gif'), move_down_color_path(color), :method => :put %>
      <%= link_to image_tag('edit_button.gif'), edit_color_path(color) %>
      <%= link_to image_tag('X_button.gif'), color, method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
    <td><%= color.created_at %></td>
  </tr>
<% end %>
</table>
<%= paginate @colors %>