<%= form_for(@coupon_category) do |f| %>
  <% if @coupon_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coupon_category.errors.count, "error") %> prohibited this coupon_category from being saved:</h2>

      <ul>
      <% @coupon_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coupon_id %><br />
    <%= f.select :coupon_id, options_from_collection_for_select(Coupon.all, :id, :name, @coupon_category.coupon_id), :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.select :category_id, options_from_collection_for_select(Category.all, :id, :name, @coupon_category.category_id), :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
