  <div class="filtered search side bar" id="filtered_search">
    <div class="filtered_navigation" id="filtered_navigation">
		  <div class="filtered_navigation_title" id="filtered_navigation_title">Refine by:</div>
      <%= filtered_navigation_system %>
    </div>
</div>
<% @body_styles.each do |body_style| %>
  <%= link_to detail_body_style_path(body_style) do %>
    <%= div_for body_style, :customer_view_of do %>
      <%= image_tag body_style.image_url if body_style.image.present? %>
      <div class="body_style_name">
        <%= body_style.name %>
      </div>
    <% end %>
  <% end %>
<% end %>
<%= top_level_browser %>
