<div id="homepage_holder">
  <div class="category carousel slide" id="featured_items_banner2">
    <div class="carousel-inner">
      <% Banner.order(:position).find_all_by_name('Featured Items Banner 2').each_with_index do |banner, index| %>
        <div class="item <%= 'active' if index == 0 %>">
          <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
        </div>
      <% end %>
    </div>
    <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
  </div>
  <div id="homepage_news">
    <div id="news_title" class="news_title_background">News &amp; updates</div>
    <div id="news_bulletins">
      <% @bulletins.each do |bulletin| %>
        <%= div_for bulletin do %>
          <header><%= link_to bulletin.title, detail_bulletin_path(bulletin) %></header>
          <div class="bulletin_like_count"><%= like_heart_for_bulletin bulletin if bulletin.og_url? %></div>
          <article>
            <% if bulletin.facebook_image_url? %>
              <div class="facebook_image">
                <% if bulletin.banner.andand.link.present? %>
                  <%= link_to image_tag(bulletin.facebook_image_url), bulletin.banner.link %>
                <% else %>
                  <%= link_to image_tag(bulletin.facebook_image_url), "##{dom_id(bulletin, :full_image)}", :class => 'fancybox' %>
                <% end %>
              </div>
            <% end %>
            <%= Redcarpet::Markdown.new(Redcarpet::Render::HTML,
    :autolink => true, :space_after_headers => true).render(bulletin.teaser.to_s).html_safe %>
            <%= link_to 'read more', detail_bulletin_path(bulletin) if bulletin.show_more? %>
          </article>
          <%= clearboth %>
        <% end %>
        <%= image_tag(bulletin.facebook_image_url, :id => dom_id(bulletin, :full_image), :class => 'full_size_bulletin_image') if bulletin.facebook_image_url? %>
      <% end %>
    </div>
  </div>
  <div id="homepage_banners">
    <div class="category carousel slide" id="featured_items_banner1">
      <div class="carousel-inner">
        <% Banner.order(:position).find_all_by_name('Featured Items Banner 1').each_with_index do |banner, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
          </div>
        <% end %>
      </div>
      <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
    </div>
    <%= clearboth %>
    <div class="small_features" id="small_features">
    </div>
  </div>
  <%= clearboth %>
  <div class="category" id="square_banners">
    <div class="carousel slide" id="square_banner1">
      <div class="carousel-inner">
        <% Banner.order(:position).find_all_by_name('Square Banner 1').each_with_index do |banner, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
          </div>
        <% end %>
      </div>
      <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
    </div>
    <div class="carousel slide" id="square_banner2">
      <div class="carousel-inner">
        <% Banner.order(:position).find_all_by_name('Square Banner 2').each_with_index do |banner, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
          </div>
        <% end %>
      </div>
      <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
    </div>
    <div class="carousel slide" id="square_banner3">
      <div class="carousel-inner">
        <% Banner.order(:position).find_all_by_name('Square Banner 3').each_with_index do |banner, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
          </div>
        <% end %>
      </div>
      <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
    </div>
    <div class="carousel slide" id="square_banner4">
      <div class="carousel-inner">
        <% Banner.order(:position).find_all_by_name('Square Banner 4').each_with_index do |banner, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= link_to(image_tag(banner.image), banner.link) if banner.image.present? %>
          </div>
        <% end %>
      </div>
      <div class="pause_button"><%= image_tag asset_path('pause_button.png') %></div>
    </div>
  </div>
  <div id="most_popular_title" class="most_popular_title">Customer Favorites</div>
  <%= link_to '#photo_uploader', :class => 'fancybox' do %>
    <div id="customer_upload_button">Upload Your Photo!</div>
  <% end %>
  <%= clearboth %>
  <div id="popular_products" class="category_of_products">
    <% @popular_products.each do |product_color| %>
      <%= product_display_box product_color %>
    <% end %>
  </div>
</div>

<div id="photo_uploader_hider">
  <div id="photo_uploader">
    <header id="photo_call_to_action">
      Show us how you wear Little Hippie®!
    </header>
    <% unless current_customer %>
      <div id="photo_coupon_offer">
        If you'd like a coupon, please <%= link_to 'log in', '#sign_in_form', :class => 'fancybox', :id => 'uploader_log_in' %> before you upload your photos.
      </div>
    <% end %>
    <%= form_for Banner.new, :remote => true do |f| %>
      <%= f.file_field :image %><br>
      <%= f.label :name, 'Name for the photo (optional)' %>
      <%= f.text_field :name %><br>
      <%= f.label :caption, 'More info (optional)' %>
      <%= f.text_area :caption %><br>
      <%= f.hidden_field :customer_uploaded, :value => true %>
      <%= f.submit 'Upload and Grant Rights to Use', :id => 'upload_and_grant' %>
    <% end %>
    <%= link_to 'View Terms of Upload', '#', :id => 'photo_upload_terms_display' %>
    <div id="photo_upload_terms">
      <em>
      Any photos you upload may be used on this website or on others.  You should only upload a photo if you have the rights to it, and if you are ready to grant Little Hippie unlimited rights to use it.  By clicking the button that says "Upload and Grant Rights to Use", you agree to the following terms.
      </em>
      <br>
      <br>
      
      <ol>
        <li>
          I have the legal right to use this photo in this way.
        </li>
        <li>
          I hereby grant Little Hippie® an unlimited license to this photo.
        </li>
        <li>
          Little Hippie® has my permission to display the photo and edit it.
        </li>
        <li>
          I understand that this photo may be displayed publicly on littlehippie.com, facebook.com, and other websites.
        </li>
        <li>
          Due to the nature of files on the Internet, it is also possible that this photo may be illegally copied for other uses.  I agree that Little Hippie® is not culpable or negligent in the case that this should happen.
        </li>
      </ol>
    </div>
  </div>
</div>
