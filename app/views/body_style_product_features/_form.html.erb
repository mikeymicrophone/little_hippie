<%= form_for(@body_style_product_feature) do |f| %>
  <% if @body_style_product_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@body_style_product_feature.errors.count, "error") %> prohibited this body_style_product_feature from being saved:</h2>

      <ul>
      <% @body_style_product_feature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body_style_id %><br />
    <%= f.select :body_style_id, options_from_collection_for_select(BodyStyle.all, :id, :name, @body_style_product_feature.body_style_id), :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :product_color_id %><br />
    <%= f.select :product_color_id, options_from_collection_for_select(ProductColor.all, :id, :name, @body_style_product_feature.product_color_id), :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
