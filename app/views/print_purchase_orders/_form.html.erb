<%= form_for(@print_purchase_order) do |f| %>
  <% if @print_purchase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@print_purchase_order.errors.count, "error") %> prohibited this print_purchase_order from being saved:</h2>

      <ul>
      <% @print_purchase_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :supplier_id %><br />
    <%= f.select :supplier_id, options_from_collection_for_select(Supplier.all, :id, :name, @print_purchase_order.supplier_id), :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :billing_address_id %><br />
    <%= f.select :billing_address_id, options_from_collection_for_select(BillingAddress.all, :id, :street, @print_purchase_order.billing_address_id), :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :garment_purchase_order_id %><br />
    <%= f.select :garment_purchase_order_id, options_from_collection_for_select(GarmentPurchaseOrder.all, :id, :name, @print_purchase_order.garment_purchase_order_id), :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
